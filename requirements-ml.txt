# Optional ML requirements (install inside .venv3.12 or Docker)
# These are heavy packages that may require toolchains or specific Python versions.

sentence-transformers>=2.2.2
torch>=2.0.0
transformers>=4.30.0
keras>=2.12.0
# Use tensorflow-cpu when GPU not required and to avoid heavy GPU deps
tensorflow-cpu>=2.11.0
spacy>=3.7.2
langdetect>=1.0.9

# spaCy small models (optional - download via `python -m spacy download`)
# python -m spacy download es_core_news_sm
# python -m spacy download en_core_web_sm

# For production ML server, install in ml_service/requirements.txt or use Docker
